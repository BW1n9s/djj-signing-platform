{
  "name": "djj-signing-bot",
  "version": "2.0.0",
  "description": "DJJ Signing Platform — Cloudflare Workers backend / CF Workers 后端",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
